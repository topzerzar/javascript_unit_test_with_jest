// Describe what to test
describe('Test Calculate', () => {

  // Test scenario
  it('sum number', () => {
    const x = 10;
    const y = 15;
    const total = x + y;
    const expected = 25;
    expect(total).toBe(expected)
  })
  // Test scenario

});
